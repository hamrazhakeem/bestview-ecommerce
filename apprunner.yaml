version: 1.0
runtime: python3
build:
  commands:
    pre-build:
      - echo "Pre-build phase"
    build:
      - echo "Build phase"
    post-build:
      - echo "Post-build phase"
run:
  runtime-version: 3.10
  command: ./start.sh
  network:
    port: 10000
    env: PORT 